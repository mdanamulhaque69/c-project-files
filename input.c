#include <stdio.h>

int main()
{
    int a, b;

    scanf("%d", &a);
    scanf("%d", &b);
    printf(" %d + %d = %d\n", a, b, a + b);

    float x, y;

    scanf("%f", &x);
    scanf("%f", &y);
    printf("%f - %f = %f", x, y, x - y);

    return 0;
}
